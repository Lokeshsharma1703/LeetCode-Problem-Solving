class Solution {
    public void reverseString(char[] s) {
        int i = 0;
        int j = s.length;
        char[] arr = new char[j];
        while(i<s.length){
            arr[--j] = s[i++];
        }
        for(i=0;i<s.length;i++){
            s[i] = arr[i];
        }
    }
}
